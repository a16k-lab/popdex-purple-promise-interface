import React from 'react';

/** Drifting light orbs for glass panels — same treatment as the Creators form. */
export const Bokeh: React.FC = () => (
  <div className="bokeh" aria-hidden>
    <span /><span /><span /><span /><span />
  </div>
);
